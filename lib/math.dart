Map<String, dynamic> SOE = {
  "1": {
    //floor number
    "P1": [
      "Start",
      "Stop",
      ["any other entry points"]
    ]
  }
};
//function to calculate the route to the location
List getpath(List coordinates) {
  List finalpath = List.empty();
  //first we solve the route to the destination floor
  //then we map the shortest route to the destination room

  return finalpath;
}
